app.title		=Sistem Database Pemasyarakatan - Ekstensi
app.name		=SDP-Ext
app.extension	=.php
app.path		=/sdp-ext
app.module.dir	=/module
app.paging.size	=50
;
; app.content.type values:
; - 0 (single): default, one single module displayed in content area
; - 1 (tabbed): module displayed per tab
;
app.content.type=0

;
; app.menu.mode values
; - 0	: tabbed toolbar mode
; - 1	: dropdown menu mode
;
app.menu.mode	=0

db.class	=-
db.url		="mysql:host=127.0.0.1;dbname=jaring"
db.username	=jaring
db.password	=x10cjaring2013
db.pool.min	=10
db.pool.max	=100
